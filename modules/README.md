# External modules

Drop a folder here containing an `index.js` (or `index.mjs`) whose default export implements the `ChinvatAdapter` contract (`hub/src/types.ts`). It is loaded at boot, defaults to tier `approve`, and appears in the dashboard like any built-in.
